import styled from 'styled-components';

import { CollapsableStateIndicator } from "../../lib/ui/CollapsableStateIndicator"
//import { getColor } from "./getters"
/* 
export const ExpandableSelectorToggle = styled(CollapsableStateIndicator)`
  font-size: 16px;
  color: ${getColor('textSupporting')};
` */

export const ExpandableSelectorToggle = styled(CollapsableStateIndicator)`
  font-size: 16px;
  color: black;
`
